{
  "name": "video-summarizer",
  "description": "Download and transcribe Instagram Reels & YouTube videos, extracting key lessons and actionable steps. Supports Gemini Flash (cloud) or Whisper (local) transcription.",
  "version": "1.2.0",
  "author": {
    "name": "Asad Qasim"
  },
  "keywords": ["video", "transcription", "youtube", "instagram", "summarizer", "gemini", "whisper"],
  "category": "productivity",
  "mcpServers": {
    "video-summarizer": {
      "command": "uv",
      "args": ["run", "${CLAUDE_PLUGIN_ROOT}/server.py"],
      "cwd": "${CLAUDE_PLUGIN_ROOT}",
      "env": {
        "PYTHONUNBUFFERED": "1",
        "UV_PROJECT_ENVIRONMENT": "~/.cache/claude-plugins/video-summarizer/.venv",
        "UV_CACHE_DIR": "~/.cache/claude-plugins/video-summarizer/cache"
      }
    }
  },
  "userConfig": {
    "GEMINI_API_KEY": {
      "description": "Optional Gemini API key for cloud-based transcription. Leave empty to use local Whisper instead. Get one at https://aistudio.google.com/apikey",
      "sensitive": true
    }
  }
}
